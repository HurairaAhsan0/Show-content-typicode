import React from 'react'

function Card() {
  return (
    <div>
      card component. and re use it.
      styling of every component.
      display user info.
      
    </div>
  )
}

export default Card
